`String → Bool`

Determines whether the all alphabetic characters in the given string are lower case characters.

Example:

	isUpper("HELLO") === false
	isUpper("hello") === true
	isUpper("hELLO") === false
	isUpper("8") === true
	isUpper("hello7") === true
