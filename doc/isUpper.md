`String → Bool`

Determines whether the all alphabetic characters in the given string are upper case characters.

Example:

	isUpper("HELLO") === true
	isUpper("hello") === false
	isUpper("hELLO") === false
	isUpper("8") === true
	isUpper("HELLO7") === true
